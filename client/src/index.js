import React from 'react';
import ReactDOM from 'react-dom';
import './App.css';  // Stylesheet
import App from './App';      // Mengimport komponen utama

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
